import { MenuItemViewPipe } from './menu-item-view.pipe';

describe('MenuItemViewPipe', () => {
  it('create an instance', () => {
    const pipe = new MenuItemViewPipe();
    expect(pipe).toBeTruthy();
  });
});
